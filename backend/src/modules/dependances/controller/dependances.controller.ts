import { Request, Response, NextFunction } from "express";
import { dependancesRepository } from "../repository/dependances.repository";

class DependancesController {
}

export const dependancesController = new DependancesController();